Release builds populate this directory with a pinned official Tor Expert Bundle using `scripts/prepare-tor.sh`.

The Tor binary is not committed to source control. Development builds may use `NAPSTR_TOR_PATH` or a `tor` executable on `PATH`.
